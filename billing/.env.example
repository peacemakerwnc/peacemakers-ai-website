# Peacemakers billing env (example — do not put real secrets here)

# Stripe (restricted key preferred; invoices + customers)
STRIPE_API_KEY=rk_live_or_sk_test_...

# Write-only inbox server (Custom GPT Action)
INBOX_SECRET=replace-with-long-random-string

# Optional — PandaDoc drafts (never commit real keys)
PANDADOC_API_KEY=

